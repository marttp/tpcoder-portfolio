---
title: "Summary after reading 'Startup Technical Guide: AI Agents' & 'Future of AI' from Google Cloud"
description: "A comprehensive summary of two Google Cloud E-books — the Startup Technical Guide for building AI agents on Google Cloud (covering models, tools, orchestration, ADK, MCP, A2A, memory, grounding, and AgentOps), and Future of AI: Perspectives for Startups 2025 (featuring insights from 20+ industry leaders on infrastructure evolution, founder advice, and where AI is heading)."
pubDate: "2026-04-12"
tags:
  [
    "AI",
    "AI Agents",
    "Google Cloud",
    "Startup",
    "Infrastructure",
  ]
heroImage: "/assets/images/blogs/summary-google-cloud-ai-ebooks/hero-image.jpg"
language: "en"
translationSlug: "summary-google-cloud-ai-agents-guide-and-future-of-ai-th"
---

import MermaidDiagram from '../../components/blog/MermaidDiagram.astro';

> **Note (April 2026):** These E-books reference the Gemini 2.5 family as the primary model. Since publication, Google has released [Gemini 3.1 Pro](https://blog.google/innovation-and-ai/models-and-research/gemini-models/gemini-3-1-pro/) (February 2026) and [Gemma 4](https://gemilab.net/en/articles/gemini-updates/gemma-4-complete-guide-2026) (April 2026). The architectural concepts, protocols (MCP, A2A), and engineering practices covered in the guides remain relevant — the model layer has just gotten better.

## Why These Two E-books

I came across two E-books from Google Cloud — one is a technical guide for building AI agents, and the other is a collection of perspectives from 20+ industry leaders on where AI is heading. I read both back-to-back because they complement each other: one tells you **how** to build AI agents, the other tells you **why** and **where** the industry is moving.

Original sources:

- [Startup Technical Guide: AI Agents](https://cloud.google.com/resources/content/building-ai-agents?hl=en)
- [Future of AI: Perspectives for Startups 2025](https://cloud.google.com/resources/content/future-of-ai-report?hl=en)

---

## Part 1: Startup Technical Guide — AI Agents

The guide opens with Sundar Pichai noting that ADK hit over 1 million downloads in its first 4 months. Google frames the agent ecosystem around three entry points: **build your own** (using Gemini models, ADK, Vertex AI), **use pre-built agents** (Gemini Code Assist, Gemini Cloud Assist, Google Agentspace), or **bring partner agents** via open protocols.

### Core Components of an AI Agent

<MermaidDiagram chart={`
flowchart TD
    A[AI Agent] --> B[Model]
    A --> C[Tools]
    A --> D[Orchestration]
    A --> E[Runtime]
    B --> B1[Gemini 2.5 Family]
    B --> B2[Model Garden - 200+ models]
    C --> C1[Extensions]
    C --> C2[Function Calling]
    C --> C3[Data Connectors]
    D --> D1[ReAct Pattern]
    D --> D2[Chain of Thought]
    E --> E1[Vertex AI Agent Engine]
    E --> E2[Cloud Run]
    E --> E3[GKE]
`} />

**Models** — Gemini 2.5 family as the primary choice, plus Model Garden with 200+ open and third-party models. The guidance: start with prompt engineering before investing in fine-tuning.

**Tools** — The guide treats tool design as an API contract — the function signature, docstring, and return schema matter because the model uses them to decide when and how to call each tool. Three categories: extensions (pre-built integrations), function calling (model invokes your APIs), and data connectors (enterprise data sources like Confluence, Jira, SharePoint).

**Orchestration** — The **ReAct (Reasoning + Acting) pattern** is the core: the model reasons about what to do, takes an action, observes the result, and repeats.

**Runtime** — Vertex AI Agent Engine (managed), Cloud Run (serverless), or GKE (full control for complex multi-agent systems).

### ADK: Agent Development Kit

The guide goes deep on agent types in Google's ADK:

<MermaidDiagram chart={`
flowchart TD
    A[ADK Agent Types] --> B[LlmAgent]
    A --> C[Workflow Agents]
    A --> D[CustomAgent]
    B --> B1["Non-deterministic\n(Model decides next step)"]
    C --> C1[SequentialAgent]
    C --> C2[ParallelAgent]
    C --> C3[LoopAgent]
    D --> D1["BaseAgent subclass\n(Full control)"]
`} />

**LlmAgent** is non-deterministic — the model decides the execution path. **Workflow Agents** provide deterministic patterns: SequentialAgent (fixed order), ParallelAgent (concurrent tasks), LoopAgent (repeat until condition met). **CustomAgent** subclasses `BaseAgent` for full control.

The recommended pattern: combine these types. Use a SequentialAgent as the outer orchestrator chaining LlmAgents for reasoning-heavy steps and workflow agents for structured steps. ADK agents deploy as FastAPI web services, containerized on Cloud Run, Vertex AI Agent Engine, or GKE.

### MCP and A2A Protocols

Two protocols for interoperability stood out to me.

**MCP (Model Context Protocol)** works in two directions — consume external tools (connect to any MCP-compatible server) and expose your tools as MCP endpoints. The analogy: MCP is to agents what USB-C is to devices.

<img src="/assets/images/blogs/summary-google-cloud-ai-ebooks/mcp-protocol-diagram.png" alt="MCP Protocol - ADK agent connecting to multiple MCP toolsets" class="rounded-lg shadow-md my-6" />

*Source: [Google Developers Blog — Developer's Guide to AI Agent Protocols](https://developers.googleblog.com/en/developers-guide-to-ai-agent-protocols/)*

**A2A (Agent-to-Agent)** enables cross-vendor agent communication. Each agent publishes an "agent card" (JSON descriptor of capabilities), communication happens through task objects, and it's modality agnostic — agents can exchange text, files, or streamed responses.

<img src="/assets/images/blogs/summary-google-cloud-ai-ebooks/a2a-protocol-diagram.png" alt="A2A Protocol - Agent discovery via agent card and task delegation" class="rounded-lg shadow-md my-6" />

*Source: [Google Developers Blog — Developer's Guide to AI Agent Protocols](https://developers.googleblog.com/en/developers-guide-to-ai-agent-protocols/)*

Together, MCP and A2A create a composable agent ecosystem — an ADK agent can consume tools via MCP, delegate subtasks via A2A, and expose its own capabilities back to the network.

### Data Architecture and Memory

This section was one of the most practical parts of the guide. It breaks data into three categories based on how agents use it — and the distinctions matter a lot when you're building agents that go beyond Q&A into taking actions with real consequences.

| Data Type | Purpose | Google Cloud Service | Real-World Example |
|-----------|---------|---------------------|-------------------|
| **Long-term knowledge** | Persistent knowledge base for RAG | Vertex AI Search, AlloyDB, Cloud SQL (pgvector), BigQuery | A customer support agent retrieves product documentation, past incident reports, and troubleshooting guides to answer tickets |
| **Working memory** | Session state, conversation context | Memorystore (Redis), Firestore | A shopping assistant remembers what items a user has been browsing during a session, their size preferences, and what they've already rejected |
| **Transactional memory** | Business-critical data with ACID | Cloud SQL, Spanner, AlloyDB | A booking agent places a hotel reservation — this needs to be durable, consistent, and never lost even if the agent session crashes |

The distinction between working memory and transactional memory matters more than it might seem. Working memory is ephemeral — lose it and the conversation restarts. Transactional memory is durable — lose it and you've lost an order, a payment, or a booking. If you're building a startup where agents take actions (not just answer questions), the transactional layer is where reliability is make-or-break.

Think of it this way: a food delivery startup building an ordering agent needs all three. Long-term knowledge stores the restaurant menus, allergen info, and delivery zone data. Working memory tracks the current conversation — "the user wants Thai food, no peanuts, delivery to Shibuya." Transactional memory is where the confirmed order lives — that record needs ACID guarantees because it triggers payment processing, restaurant notifications, and driver dispatch.

One concept new to me: **Vertex AI Memory Bank** and memory distillation. Instead of keeping raw conversation history (which grows linearly and gets expensive), the system distills conversations into structured memory entries — key facts, user preferences, learned patterns. Two approaches: automated distillation (system extracts info) and agent-directed distillation (agent explicitly decides what to remember). For a startup, this means your agent can "learn" that a returning customer always orders extra spicy and prefers contactless delivery — without storing every past conversation verbatim.

### Grounding: RAG Evolution

Grounding is about giving agents access to accurate, up-to-date information so they don't hallucinate. The guide covers an evolution across three approaches, each building on the last.

**RAG (Retrieval-Augmented Generation)** is the baseline. The pipeline from the guide:

<MermaidDiagram chart={`
flowchart LR
    A[Source] --> B[Parsing & Enriching]
    B --> C[Transformation\ne.g. Chunking]
    C --> D[Embedding & Indexing]
    D --> E[(Vector DB)]
    E --> F[Retrieval]
    F --> G[Ranking]
    G --> H[Serving]
    H --> I[LLM API]
`} size="medium" />

Data goes in through parsing, chunking, and embedding into a vector database. At query time, the user's question is embedded, the vector DB finds semantically similar content, results are ranked, and the most relevant chunks are served to the LLM as context.

The guide uses a shoe company as an example: product descriptions, warranty info, and FAQs are embedded and stored. When a customer asks about "shoes good for people with wide feet," the vector database understands that's semantically related to "extra wide," "comfortable for wide feet," and returns relevant results — something a traditional `LIKE '%good for people with wide feet%'` SQL query would miss entirely. The difference between keyword matching and meaning-based search is the core value of RAG.

<MermaidDiagram chart={`
flowchart TB
    subgraph VectorDB["Vector Database Use Case"]
        direction LR
        Q["🔍 User Query:\n'shoes good for people\nwith wide feet'"]
        Q --> VDB[(Vector DB\nSemantic Search)]
        VDB --> R1["extra wide"]
        VDB --> R2["comfortable for wide feet"]
        VDB --> R3["wide fit, extra wide"]
    end

    subgraph Traditional["Traditional SQL"]
        direction LR
        TQ["LIKE '%good for people\nwith wide feet%'"]
        TQ --> TR["❌ No results"]
    end
`} size="medium" />

On Google Cloud, this maps to Vertex AI RAG Engine for the pipeline and Vertex AI Vector Search for storage and retrieval. The guide also recommends a **retrieve-and-re-rank** approach: first retrieve a larger-than-needed set of candidate documents (high recall), then pass them through a re-ranking service that filters out irrelevant or semantically opposite results (high precision).

**GraphRAG** extends RAG with knowledge graphs. Instead of just matching similar phrases, the agent understands how concepts relate to each other. The guide's example: a medical AI assistant that needs to know "symptoms → causes → treatments" — not just retrieve related snippets, but traverse relationships between entities.

<img src="/assets/images/blogs/summary-google-cloud-ai-ebooks/graphrag-architecture.jpg" alt="GraphRAG architecture — data ingestion subsystem with Cloud Storage, Pub/Sub, Cloud Run file processor generating embeddings and knowledge graph in Cloud Spanner, and serving subsystem with ADK agent through Vertex AI Agent Engine" class="rounded-lg shadow-md my-6" />

*GraphRAG architecture on Google Cloud — from the Startup Technical Guide (p.19)*

The architecture from the PDF shows two subsystems:
- *Data ingestion*: files land in Cloud Storage → Pub/Sub queues them → Cloud Run processes them → generates both embeddings and a knowledge graph → stored in Cloud Spanner
- *Serving*: ADK agent queries through Vertex AI Agent Engine, using both the Embedding API and Gemini API to traverse the graph and generate responses

This is where BioCorteX's drug discovery use case fits. Connecting research papers, molecular structures, and clinical trial data through relationship graphs lets the agent answer questions like "what drugs interact with compound X given patient condition Y" — questions that require traversing relationships, not just finding similar documents.

**Agentic RAG** is the most advanced approach. The agent becomes an active participant in retrieval rather than a passive recipient. Following the ReAct pattern, the agent can analyze a complex query, formulate a multi-step plan, and execute multiple tool calls in sequence to find the best information.

The guide's example: a customer asks "Are the SolarFlare runners in stock?" The agent (1) uses semantic search via `product_lookup` to identify the specific shoe model even if the customer describes it vaguely, then (2) triggers `check_inventory` via function calling to get real-time stock data — returning "Yes, we have 82 pairs available right now." This combines retrieval (knowing what information is relevant) with actions (performing real-time operations).

<MermaidDiagram chart={`
flowchart LR
    User["👤 User:\n'Are the new SolarFlare\nrunners in stock?'"]
    User --> F1
    subgraph Functions
        F1["product_lookup"]
        F2["check_inventory"]
    end
    subgraph Tools
        T1["Vector database"]
        T2["Inventory system"]
    end
    F1 --> T1
    F2 --> T2
    Tools --> Agent
    Agent["🤖 Agent:\n'Yes! The SolarFlare runners\nare in stock. We have 82 pairs\navailable right now.'"]
`} size="medium" />

The guide also covers other grounding methods beyond RAG: **Google Search grounding** (connect to real-time web knowledge), **Google Maps grounding** (location-aware responses), and **grounding with Elasticsearch** (use existing search indexes with Gemini).

### AgentOps: Production Readiness

The guide frames AgentOps as the adaptation of DevOps, MLOps, and DataOps principles for AI agents. The opening line of this section stuck with me: moving beyond superficial "vibe-testing" requires a rigorous engineering approach. If you've ever shipped a microservice to production, you know the gap between "it works on my machine" and "it runs reliably at scale." Agent systems have the same gap, but with an extra dimension: non-determinism.

The guide introduces a **4-layer evaluation framework**:

| Layer | What It Evaluates | Traditional Equivalent |
|-------|-------------------|----------------------|
| **Layer 1: Component** | Individual tools, prompts, model responses | Unit tests — does each function work with valid, invalid, and edge-case inputs? Same as testing a REST endpoint handler or a data parser. |
| **Layer 2: Trajectory** | Did the agent take the right steps? (procedural correctness) | Integration tests — did the request flow through the right services in the right order? In agent terms: did the ReAct loop follow a logical Reason → Act → Observe sequence? |
| **Layer 3: Outcome** | Is the final result correct? (semantic correctness) | End-to-end tests — is the API response correct? For agents: is the answer factually accurate, grounded, and helpful? |
| **Layer 4: System-level** | Production behavior at scale | Production monitoring — latency percentiles, error rates, resource usage. For agents: tool failure rates, ReAct cycle counts per task, token usage, cost per interaction. |

What makes this layered approach valuable is that each layer catches different failure modes. Layer 1 catches broken tools. Layer 2 catches agents that arrive at the right answer through wrong reasoning (which will fail on harder cases). Layer 3 catches hallucination and factual errors. Layer 4 catches production drift, cost spikes, and latency degradation.

If you've worked with observability in traditional backend systems, the parallel is direct. Layer 4 maps to what you'd set up with Prometheus/Grafana or Datadog for a microservice — but instead of tracking HTTP status codes and p99 latency, you're tracking tool call success rates, number of ReAct cycles per task, and token consumption. ADK integrates with Cloud Trace and OpenTelemetry natively, so each Reason, Act, and Observe step in the ReAct loop becomes a trace span you can inspect — similar to how distributed tracing works across microservices with Jaeger or Zipkin.

The guide emphasizes that any change to the agent's code, tools, or prompts should trigger automated evaluation against a predefined dataset. This is the CI/CD equivalent of running your test suite on every commit, but adapted for non-deterministic systems. The five-step workflow from the PDF:

1. **Bootstrap** with Agent Starter Pack (generates Terraform, Cloud Build configs, evaluation datasets)
2. **Develop** with ADK (write agent logic, tools, instructions)
3. **Commit and automate** (Cloud Build CI/CD pipeline triggers automatically)
4. **Evaluate continuously** (pipeline builds the agent, runs quantitative evaluation against test set)
5. **Deploy confidently** (only validated versions reach production)

The **Agent Starter Pack** bundles this into a production-ready template:

| Component | Tool | Purpose |
|-----------|------|---------|
| Infrastructure as Code | Terraform | Provision Cloud Run, IAM, networking |
| CI/CD | Cloud Build | Automated build, test, evaluate, deploy |
| Observability | Cloud Trace + OpenTelemetry | Distributed tracing of agent decisions |
| Logging | Cloud Logging | Centralized log management |
| Analytics | BigQuery | Store and analyze agent interaction data |
| Monitoring dashboards | Looker Studio | Visualize agent performance trends |
| Evaluation | Vertex AI Evaluation | LLM-as-judge scoring, human-in-the-loop feedback |

You can bootstrap a new project with a single command: `uvx agent-starter-pack create my-agent -a adk@gemini-fullstack`.

<img src="/assets/images/blogs/summary-google-cloud-ai-ebooks/agent-starter-pack-architecture.png" alt="Agent Starter Pack - High level architecture covering evaluation, LLMs, orchestration, deployment, observability, and CI/CD" class="rounded-lg shadow-md my-6" />

*Source: [Google Cloud Platform — Agent Starter Pack](https://github.com/GoogleCloudPlatform/agent-starter-pack)*

The responsible AI section also covers security through Google's SAIF framework. The approach mirrors defense-in-depth from traditional security engineering: secure infrastructure (Terraform provisions hardened environments with specific IAM roles), input guardrails (validate prompts for injection attacks), output guardrails (filter agent responses for harmful content), and auditing (every agent thought and tool call is traced and logged to BigQuery for compliance reviews).

### Google's Pre-built Agent Products

Beyond building your own, Google offers ready-to-use agents:

- **Gemini Code Assist** — AI coding assistant integrated into IDEs. Goes beyond code completion into code transformation, test generation, and code review. Handles full repository context for large-scale refactoring.
- **Gemini Cloud Assist** — Helps with Google Cloud operations — architecture design, troubleshooting, and optimizing cloud resources. Useful for reducing the learning curve when working with unfamiliar GCP services.
- **Google Agentspace** — Enterprise platform for governing and orchestrating an agent workforce. Includes an Agent Gallery (catalog for publishing/discovering internal agents), no-code Agent Designer, and NotebookLM Plus. Connects to 100+ enterprise data sources (Workspace, Confluence, Jira, Salesforce, ServiceNow, SharePoint, etc.).
- **Firebase Studio** — Full-stack development workspace with an App Prototyping Agent that generates complete applications from natural language descriptions.
- **Gemini CLI** — Open-source terminal agent with free Gemini access for developers who prefer working in the terminal.

### Customer Stories

Three case studies: **BioCorteX** uses GraphRAG + A2A for drug discovery (multiple specialized agents collaborate on molecular and clinical data). **Box** uses A2A for content management workflows. **Zoom** integrates with Google Agentspace for meeting scheduling.

---

## Part 2: Future of AI — Perspectives for Startups 2025

Insights from 20+ contributors including Amin Vahdat (Google), Harrison Chase (LangChain), Elad Gil, Jennifer Li (a16z), Arvind Jain (Glean), Douwe Kiela (Contextual AI), Edo Liberty (Pinecone), Jerry Chen (Greylock), Mayada Gonimah (Thread AI), and others.

### Infrastructure Evolution

Amin Vahdat outlines five shifts: application-specific computation (TPUs at ~10x efficiency), networking specialization, the memory wall, compute packing under power constraints, and liquid cooling as standard. The takeaway: plan for cheaper compute, not today's cost constraints.

### Key Themes

**Models improving faster than expected** — Douwe Kiela frames a "multi-LLM future" where different models excel at different tasks.

**Agents as the next paradigm** — Harrison Chase identifies five areas: UX beyond chat, human-in-the-loop as a spectrum, vertical-specific agents, context learning, and low-level frameworks. His closing: "Agents hold the key... but success depends on our guidance."

**Application layer captures value** — As models commoditize, domain expertise, data access, and UX become the differentiators.

### Advice for Founders

The recurring recommendations across contributors:

- **Move fast and ship** — over-planning is a bigger risk than under-planning
- **Focus on niche problems** — vertical AI (legal, healthcare, logistics) beats general-purpose
- **Value-based pricing** — price on value delivered, not inference cost
- **Prioritize evaluations** — if you can't measure it, you can't improve it
- **AI for revenue, not just cost reduction** — new revenue streams matter more than cost cutting
- **Build model-agnostic** — the ability to swap models is a strategic advantage
- **Human-in-the-loop** — knowing when to escalate to a human is a feature, not a limitation
- **Stickiness over demos** — workflow integration that becomes embedded beats impressive one-off demos

Mayada Gonimah's perspective resonated with me: building AI agent systems requires the same fundamentals as building distributed systems — managing state, handling failures, ensuring consistency, dealing with concurrent operations. The tooling differs, but the engineering discipline is the same.

---

## What I Took Away

1. **The agent paradigm is not hype** — Google's technical guide covers ADK agent types, deployment patterns, memory distillation, 4-layer evaluation frameworks. This is engineering, not marketing.

2. **Data architecture matters more than model choice** — long-term knowledge, working memory, transactional memory, memory distillation. The data layer is where most production agent systems struggle.

3. **Evaluation is the underinvested layer** — Both E-books emphasize it. The technical guide has a structured 4-layer framework. Multiple founders independently said "prioritize evaluations."

4. **Interoperability is a strategic bet** — MCP, A2A, multi-model support. Building for flexibility now saves painful migrations later.

5. **Distributed systems skills transfer** — For backend engineers, the shift toward agents means managing state across interactions, handling tool integrations, building evaluation pipelines, and operating multi-agent systems. The skills are the same.

---

## Sources

- [Startup Technical Guide: AI Agents — Google Cloud](https://cloud.google.com/resources/content/building-ai-agents?hl=en)
- [Future of AI: Perspectives for Startups 2025 — Google Cloud](https://cloud.google.com/resources/content/future-of-ai-report?hl=en)
- [Developer's Guide to AI Agent Protocols — Google Developers Blog](https://developers.googleblog.com/en/developers-guide-to-ai-agent-protocols/)
- [Agent Starter Pack — Google Cloud Platform GitHub](https://github.com/GoogleCloudPlatform/agent-starter-pack)
