---
title: "Designing an Audience Platform (Start Simple, Then Evolve)"
description: "Grow a send-time SQL query into an audience platform — nightly batch, Spark over a data lake, near-real-time streaming — that serves one audience logic in two shapes: bulk chunks for delivery and a per-user membership store for serving."
pubDate: "2026-07-08"
tags:
  ["Software Architecture", "Software Engineering", "Backend"]
heroImage: "/assets/images/blogs/design-audience-platform/audience-two-shapes.png"
language: "en"
translationSlug: "design-audience-platform-th"
servicesNote: true
---

import MermaidDiagram from '../../components/blog/MermaidDiagram.astro';

## Introduction

This is the sixth post in the backend system design series — same format: start with the most naive thing that works, then let real-world constraints push it into shape.

This one is the system the last two posts kept pointing at. In the omnichannel post (#3) we said "consume a *prepared, chunked* audience — don't re-derive it here, that's a different system." In the icon-serving post (#5) we said eligibility "comes from the Audience domain — a rule engine that writes a per-user membership store, its own post, next in the series." This is that post. The Audience Platform answers one question — **who?** — and hands the answer to whoever needs it.

The interesting part is *whoever needs it*, because the two consumers we've already built want the answer in completely different shapes:

- **Bulk, chunked list** — the omnichannel backbone wants "campaign C targets these 40M users," split into ~40–60K chunks it can stream through its delivery workflow. It reads the whole audience, once, in order.
- **Per-user membership store** — the icon serving path wants a point lookup at request time: "is user `10001` eligible for campaign 5, right now?"

Same audience logic — *gold-tier users in TH who topped up in the last 30 days* — two output shapes with opposite access patterns. That contrast is the spine of this post, the same way "two systems, opposite SLAs" was the spine of the icon post. One brain, two mouths.

<MermaidDiagram chart={`
flowchart LR
    R["Audience logic\n(rules over user data)"] --> A["Bulk chunked list\n(#3 Omnichannel)"]
    R --> B["Per-user membership store\n(#5 Icon serving)"]
`} />

> **P.S.** Icon serving is the concrete example, carried over from post #5, but treat it as a stand-in. Whatever you're building, if it asks "is this one user eligible right now," it reads the membership store the same way.

Campaigns are the running example, but the **who?** question shows up far beyond marketing. The same platform, unchanged, also backs:

- **Feature rollouts** — ship a new feature to a small segment first ("gold-tier in TH"), then widen it.
- **Experimentation** — put users into A/B test groups and keep those groups stable across sessions.
- **Personalization** — decide which banner, recommendation shelf, or offer a user sees, per segment.
- **Risk and compliance lists** — fraud watchlists and opt-out suppression: the same membership lookup, used to *exclude* instead of include.

It also appears under other names: the segment engine inside a **CDP** (Customer Data Platform), a **DMP** (Data Management Platform) in ad tech, or the targeting part of a feature-flag service. Different labels, same design.

Here's the road. The first half is the evolution of *how we compute the audience*; the payoff at the end is *how we serve it two ways*.

**Lower scale — a single box still works:**

1. **Inline SQL** at send time — works until the query is 40M rows mid-send
2. **Precompute on a schedule** — build the segment ahead of time, read it at send
3. **A read replica** — squeeze what a single box can do before going distributed
4. **An analytical engine** — OLTP stops being enough once the workload is real analytics

**Larger scale — now we're in distributed-system territory:**

1. **A data lake** — land data from a dozen systems in one place built for analytical reads
2. **Spark** — rules become code once SQL can't express the logic anymore
3. **Near-real-time** — "nightly" isn't fresh enough for some rules
4. **A reusable segmentation service** — stop building a custom job per use case

Let's start simple.

---

## The naive version — a SQL query at send time

You want to send a promo to *gold-tier users in Thailand who topped up in the last 30 days*. The honest first version writes exactly that, as a query, at the moment of sending:

```sql
SELECT user_id
FROM users
WHERE tier = 'gold'
  AND country = 'TH'
  AND last_topup_at > now() - interval '30 days';
```

The sender runs it, gets a list of IDs, and starts pushing. For a few thousand users this is completely fine — ship it. The rule lives in one place, it's always fresh (the query runs against live data), and there's nothing to operate.

> **P.S.** Stop here if that's you. Under roughly 1,000 users with a rule this simple — one or two conditions, no cross-table joins — this inline query isn't a temporary shortcut, it's the right answer. Nothing in the sections below makes the system more *correct*; they make it survive scale and complexity you don't have yet. Add machinery when the cracks below actually show up, not before.

Then it grows, and three cracks open at once:

- **The query gets heavy.** Past around a million rows, with `last_topup_at` from a different table and `tier` derived from yet another, this becomes a multi-join full scan on your **production** database — the same database serving live traffic. One marketing send can stall the app.
- **The send blocks on it.** The delivery job can't start until the query returns. A query that takes minutes (or times out) means the campaign sits there waiting, and a retry re-runs the whole expensive thing.
- **You can't reuse it.** The next campaign wants "gold-tier in TH" too, but with a different topup window. Every campaign re-derives its audience from scratch, hammering the DB again, and nobody can answer "how big is this segment?" without running the query.

All three say the same thing: **deriving the audience at send time, on the live DB, doesn't scale.** The fix is to move the computation off the hot path and do it *ahead* of the send.

{/* IMAGE PROMPT (send-time-query-load.png): Clean conceptual diagram, flat vector, light background. CENTER: a database cylinder labeled exactly "Production DB", drawn under visible strain (a couple of small stress marks). From the LEFT, an arrow labeled exactly "Marketing query" points into the database (draw it thick/heavy). Going DOWN from the database to a small row of phone icons labeled exactly "Live users", draw a thin struggling arrow with a tag reading exactly "slow". Convey: a heavy marketing query on the live database starves real user traffic. STRICT: render ONLY these labels — "Production DB", "Marketing query", "Live users", "slow" — spelled exactly, each once; phone icons carry no text; no other words; no typos; no duplicate boxes; no meta/style words. Flat vector, soft shadows, 2-3 accent colors, no photorealism. */}

![A heavy marketing query on the production database starves the live-user traffic it shares](/assets/images/blogs/design-audience-platform/send-time-query-load.png)

> **P.S.** Before reaching for "precompute it ahead of time," though, it's worth asking honestly: how fresh does this audience actually need to be? If a user losing gold tier at 9am can still see the promo until the next scheduled run, that lag is acceptable — precompute solves everything in the next few sections. If eligibility has to flip the moment the underlying fact changes — a fraud flag, a balance crossing a threshold — no amount of precomputing on a schedule gets you there, and you're really looking at the near-real-time path later in this post, not this one. Most audience use cases tolerate the lag, which is why precompute is the right next step for most people — but that's a decision worth making on purpose, not by default.

---

## Precompute on a schedule — build the segment, then read it

**The problem:** the audience query is expensive and we run it at the worst possible moment — right when we need to send. But the inputs barely change minute to minute. "Gold tier in TH, topped up in 30 days" is basically the same list at 2pm as it was at 2am.

**The fix:** run the query *ahead of time* on a schedule, store the result, and let send time just **read** it. A nightly job computes each segment's members and writes them to a `segment_members` table:

```sql
-- nightly, per segment: delete + insert in ONE transaction,
-- so a re-run or a retry rebuilds the segment instead of doubling it
BEGIN;
DELETE FROM segment_members WHERE segment_id = 'gold_th_active';
INSERT INTO segment_members (segment_id, user_id, computed_at)
SELECT 'gold_th_active', user_id, now()
FROM users
WHERE tier = 'gold' AND country = 'TH'
  AND last_topup_at > now() - interval '30 days';
COMMIT;
```

Two boring details in that snippet are doing the real work:

- The job is **idempotent** — a bare `INSERT` alone is the classic mistake here, because the first retry after a crash doubles every member; the delete-and-insert in one transaction (with a primary key on `(segment_id, user_id)` as the backstop) makes re-running always safe.
- Once the audience is precomputed it can silently go stale, so **alert on the age of `max(computed_at)`, not just on job failure** — a nightly job that dies quietly on Friday night means a three-day-stale audience by Monday, and "the job didn't page" is not the same as "the data is fresh."

Now a send is a cheap read: `SELECT user_id FROM segment_members WHERE segment_id = 'gold_th_active'`. The heavy lifting happened overnight, when nobody's waiting.

<MermaidDiagram chart={`
flowchart LR
    CRON["Nightly job"] --> Q["Run segment query"]
    Q --> SM[("segment_members")]
    SEND["Send time"] -->|"cheap read"| SM
`} />

This buys a lot — the send is now a simple `segment_id` lookup instead of a multi-join scan, the expensive query runs off-peak, and a segment is now a *thing* you can size and reuse. But it trades one problem for two new ones:

- **Freshness.** The list is only as fresh as the last rebuild. Run this batch job (a cron job) once a day, and the list can be up to 24 hours stale — it doesn't matter if the job runs at midnight, 1am, or 3am, what sets the staleness is how often it runs, not the exact time. A user who topped up this morning won't be "gold-tier active" until the next rebuild. For a weekly promo that's fine; for "remind users who *just* abandoned a cart," it's useless. Keep this in mind — it's what forces streaming later.
- **It still runs on the production DB, and it's still one box.** The rebuild needs somewhere to land: a new table, `segment_members(segment_id, user_id, computed_at)`, written by a job that still hits the same Postgres serving live traffic. And that table isn't small — a user can match more than one segment, so worst case it holds **S × N** rows (S = segments in the org, N = total users). As segments multiply and rules get more complex (cross joins, windowed aggregates over a year of transactions), a single box doing both live traffic and this analytical batch starts to slow down and fail.

Worth naming the cheapest possible fix for freshness, before reaching for anything else: just run the job more often. Nothing stops you from running the same full delete-and-insert every hour instead of once a night — same production DB, same table, zero new infrastructure, just a shorter interval on the same cron schedule. You trade a bit more compute for a lot more freshness, for free.

{/* IMAGE PROMPT (freshness-cadence.png): Clean conceptual diagram, flat vector, light background. Two stacked horizontal timelines that share a single left-to-right arrow as the time axis. TOP timeline labeled exactly "Nightly": a single refresh dot at the far left, then a long empty stretch, with a tag reading exactly "up to 24h stale". BOTTOM timeline labeled exactly "Microbatch": many evenly spaced refresh dots along the line, with a tag reading exactly "fresh within 1-2h". Convey: frequent refreshes stay fresh versus one nightly rebuild. STRICT: render ONLY these labels — "Nightly", "up to 24h stale", "Microbatch", "fresh within 1-2h" — spelled exactly, each once; the dots carry no text; no other words; no typos; no duplicate labels; no meta/style words. Flat vector, soft shadows, 2-3 accent colors, no photorealism. */}

![Nightly leaves the audience up to a day stale; running the same rebuild more often keeps it fresh within an hour or two](/assets/images/blogs/design-audience-platform/freshness-cadence.png)

That works until the full rebuild itself takes too long to finish inside the window — recomputing everything from scratch every hour eventually costs more than an hour has. That's the trigger for the *incremental* refresh this series comes back to once there's a data lake to lean on.

The second crack is the structural one. We don't just need to precompute — we need to precompute *somewhere that isn't the live transactional database*. But "somewhere else" doesn't mean a cluster yet, and this next rung is the one most write-ups skip.

---

## Squeeze the single box first — a read replica

Most posts jump from "one Postgres" straight to "a Spark cluster," and readers walk away believing distributed compute is step three. It isn't. There's a cheap move first, and — being honest — a lot of companies could stop here forever.

**Get the segment job off the primary — for reads, and for where it lands.** Right now production is doing two different jobs: serving live transactional traffic, and holding the `segment_members` table this batch writes into. Split them apart:

- **Reads** — point the batch's queries at a **read replica**: a live copy of the primary, kept in sync through replication, that only accepts reads and never writes. Because it's read-only, it can absorb read traffic from anywhere — the live app's own reads as well as this batch job's — while every write, from the app or from this job, still has to go through the primary. Nearly every managed database offers a replica as a checkbox, and replication lag of a few seconds is irrelevant for a marketing audience that already tolerates hours of staleness.
- **Output** — don't write `segment_members` back into that same box. Land it somewhere else entirely: a small dedicated database used only for segments, a plain file in object storage (S3/GCS) the sender reads directly, or Redis holding the segment as a set.

None of these need Spark or a warehouse yet — the computation is still the same SQL query, just moved so production only does the one job it should: live traffic. This is a cheap infrastructure change that buys years at many companies — take it *early*, the moment the batch visibly competes with live traffic.

<MermaidDiagram chart={`
flowchart LR
    APP["Live traffic"] --> PRIMARY[("Production primary")]
    PRIMARY -->|"replicate"| REPLICA[("Read replica")]
    BATCH["Batch job"] -->|"read"| REPLICA
    BATCH -->|"write"| EXT[("External store\ndedicated DB / S3 file / Redis")]
    SEND["Send time"] -->|"read"| EXT
`} />

> **P.S.** Honestly, this might be as far as you need to go. If your segment output already lives outside production — in Redis, a flat file, or its own small database — and a send is just a simple lookup, most teams can stop right here. The next section solves a different problem: the read query itself getting too heavy even for a replica, not where the result is stored.

**Before going further, a quick recap.** Two consumers, two read patterns, and neither cares which engine produced the list:

- **Omnichannel** — wants the whole audience in one **chunk read**, in order.
- **Icon serving** — wants a single user's eligibility as a **real-time read**, a point lookup right now.

<MermaidDiagram chart={`
flowchart LR
    E1["Primary / replica"] -.-> SEG[("Segment store\n(table, file, redis)")]
    SEG -->|"chunk read"| OMNI["Omnichannel"]
    SEG -->|"real-time read"| ICON["Icon serving"]
`} />

Both just read whatever lands in the segment table. That's why this rung works without changing anything downstream: the *shape* of the output never changes, only how it gets computed. Next: what happens when the compute itself stops being enough.

> Most applications in the world can stop right here. Keep that in mind.

---

## Move the analytics to an analytical engine — from OLTP to OLAP

Same move as the last rung — precompute, then read from somewhere that isn't the hot path — just pointed at something else. Instead of reading from a replica, read from an **OLAP** engine.

Here's why. Once rules become 90-day windowed aggregates and multi-way joins over hundreds of millions of rows, even a replica starts taking hours — not because the hardware is weak, but because this is real analytical work now. A replica is still an **OLTP** engine (Postgres/MySQL: built for many small point reads and writes, one row at a time). **OLAP** engines are built for the opposite — scanning a few columns across millions of rows at once. That's the fix, and it's still not a cluster:

- **Columnar analytical database** — ClickHouse, or DuckDB on one beefy machine.
- **Managed warehouse** — BigQuery, Snowflake, Redshift, Athena.

Either runs the same kind of query one or two orders of magnitude faster. "A scheduled SQL query on the warehouse, output written to a segment table" is — quietly — the mainstream audience platform in 2026. Plenty of teams run exactly that at tens of millions of users and never need more.

> **P.S.** If your team is already Java/Spring, there's a lever worth pulling before reaching for a separate OLAP store: scale the batch job itself with **Spring Batch**. Chunked reads, transactions, checkpointing, restart-from-failure, and remote partitioning to spread the work across more than one JVM if needed — it can push a read replica further than a naive query would, with no new storage engine involved. It stops being enough at the same trigger as the rest of this rung: once the workload is genuinely OLAP-shaped, or once you need Spark's built-in distributed joins across billions of rows, Spring Batch doesn't have those baked in.

Worth being precise here, so "just point at something else" doesn't undersell what changed. A read replica and an OLAP engine solve *different* problems:

- **Replica** — same OLTP engine, just a synced copy. It only separates *where reads happen* from *where writes happen*; the data itself never leaves the OLTP world, and staying in sync is native database replication.
- **OLAP engine** — a different storage engine entirely, built to hold data in a shape suited for analytics. It doesn't fill itself through replication — something has to move the data in: an initial bulk copy, a scheduled Spark job, or CDC (streaming the OLTP write log into the OLAP store continuously).

Same job, the same *kind* of query — but a different source, and populating that source is its own problem. (The exact SQL text usually needs rewriting too — Postgres, ClickHouse, BigQuery, Snowflake, and Redshift each speak their own SQL dialect.) If you're not already running an OLAP store, a replica is the simpler, honest answer. If you do reach for OLAP, that data-movement problem is exactly the ingestion story the data lake section covers in full.

**So when do you actually leave a rung?** Rough triggers, not fixed rules:

| You're on | Move up when |
|---|---|
| Inline SQL at send time | audiences pass ~1M users, campaigns multiply, or a send query first slows the app |
| Precompute on the primary | the batch visibly competes with live traffic — the replica is one config change, take it |
| Read replica | analytical SQL takes hours on a row store — the shape is wrong, not the hardware |
| Warehouse / columnar DB | jobs can't finish inside their refresh window, rules need real *code* (custom scoring, ML features) rather than SQL, or the inputs no longer live in one database |

One more dial: **every rung up adds ongoing cost and on-call work.** A replica is a checkbox; a warehouse is a bill; the Spark-plus-lake world ahead is a bill *and* a team. Climb because a trigger fired, not because the next rung looks interesting on a resume.

If you're on a managed warehouse and it's coping, skim the next two sections. The series follows the self-managed path from here — the data lake, then Spark on top of it — because that's where things break in interesting ways at the largest scale, and because past this point, efficient progress needs a data lake no matter which engine got you here.

---

## Land the data in one place — the data lake

**The problem:** the data an audience rule needs now lives in a dozen systems — the payments DB, the auth service, data-science file exports, an event stream. Querying each one live, for every segment build, is the load problem again — an analytical job hammering every production system at once. These jobs also need *history* ("transactions over 90 days") that hot OLTP databases prune away.

**The fix:** land a copy of everything in one place built for analytical reads — a **data lake**. Production gets queried once, for ingestion — never again for compute.

**Columnar format — Parquet.** Store data by column, not row. A query touching 3 of 30 columns only reads those 3 off disk, and Parquet's min/max stats let it skip whole chunks that can't match (**predicate pushdown**). On a wide table, that means reading 10 times less data off disk than a row format would — often more.

{/* IMAGE PROMPT (parquet-columnar.png): Clean conceptual comparison, flat vector, light background, two panels side by side, each showing a simple table grid of blank cells. LEFT panel header labeled exactly "Row storage": highlight whole HORIZONTAL rows across the grid, with a tag below reading exactly "reads every column". RIGHT panel header labeled exactly "Columnar (Parquet)": on an identical grid, highlight only a few VERTICAL columns, with a tag below reading exactly "reads only what it needs". Convey: columnar storage reads only the needed columns off disk. STRICT: render ONLY these labels — "Row storage", "reads every column", "Columnar (Parquet)", "reads only what it needs" — spelled exactly, each once; grid cells contain no text; no other words; no typos; no duplicate labels; no meta/style words. Flat vector, soft shadows, 2-3 accent colors, no photorealism. */}

![Row storage reads every column off disk; columnar Parquet reads only the columns the query needs](/assets/images/blogs/design-audience-platform/parquet-columnar.png)

**Table format — Iceberg / Delta Lake.** Raw Parquet files alone have no notion of a transaction — a reader can catch a half-written table, schema changes are manual, "what did this look like yesterday?" is unanswerable. A table format adds **ACID snapshots**, **schema evolution**, and **time travel** (query a past snapshot) — worth the extra infrastructure once your output drives money-spending campaigns.

**Partitioning.** Partition by **event date** (`dt=YYYY-MM-DD`) so a 90-day query reads 90 folders, not the whole history. Don't partition by something high-cardinality like `user_id` — millions of tiny files, metadata bigger than the data itself (the **small-files problem**). Run **compaction** to merge them back down.

**Ingestion — getting data in.** Usually both: **batch snapshots** (a daily dump, simple, a day stale) and **CDC** (stream the source DB's write-ahead log via Debezium → Kafka, minutes stale instead of a day — also where streaming later starts from).

**Orchestration.** Ingest → transform → build-segments → publish, each depending on the last. An orchestrator (Airflow, Dagster) runs it on schedule and won't start a segment build before that day's inputs land.

<MermaidDiagram chart={`
flowchart LR
    SRC1[("Payments DB")] -->|"CDC / snapshot"| LAKE[("Data lake\nParquet + Iceberg")]
    SRC2[("Auth / profile")] -->|"CDC / snapshot"| LAKE
    SRC3[("DS churn scores")] -->|"batch"| LAKE
    EV["Event stream"] -->|"Kafka"| LAKE
`} />

Freshness vs. cost trades off right here: daily batch is cheap and simple but a day behind; CDC is fresh but real infrastructure — Debezium, Kafka, compaction, an orchestrator, all wanting monitoring and someone to page. This is the rung where the platform gets a team, which is why the OLAP section told you to wait for a real trigger first. Most platforms land in the middle: some tables daily, others within the hour.

Now there's one place to read from, without ever touching production again. What still can't be expressed here is the *logic* itself — that's next.

---

## Rules become code — move to Spark

**The problem:** even with everything in one lake, the warehouse rung still has a ceiling — not size, but *shape*. Real targeting looks like "users whose **average** transaction over 90 days is above X, who used feature Y at least 3 times, are **not** suppressed, joined against a churn score from data science." Windowed aggregates and anti-joins are easy for a warehouse — but the scoring logic the business actually wants is *code*, not a `WHERE` clause. Once rules stop being expressible in SQL, you need an engine built for arbitrary logic as the normal case, over hundreds of millions of users and *billions* of rows.

**The fix:** move the computation to a distributed engine — Spark. It processes billions of rows in parallel across a cluster, and (the part I care about most) lets you write the rule once while the engine handles the distribution.

**The job, in DataFrame terms.** Read the inputs, apply predicates, join attributes, aggregate, write membership:

```scala
val users   = spark.read.parquet("s3://lake/users/dt=2026-07-01")        // today's snapshot
val txns    = spark.read.parquet("s3://lake/transactions")               // partitioned by event_date
val churn   = spark.read.parquet("s3://lake/churn_scores/dt=2026-07-01") // today's snapshot
val suppress = spark.read.parquet("s3://lake/suppression/current")

val txn90 = txns
  .where($"event_date" >= date_sub(current_date(), 90))
  .groupBy($"user_id")
  .agg(avg($"amount").as("avg_amt"), count("*").as("txn_count"))

val audience = users
  .where($"tier" === "gold" && $"country" === "TH")
  .join(txn90, Seq("user_id"))
  .join(broadcast(churn), Seq("user_id"))           // small dim → broadcast
  .where($"avg_amt" > 5000 && $"churn_score" < 0.3)
  .join(suppress, Seq("user_id"), "left_anti")       // exclude suppressed
  .select($"user_id")
```

**Broadcast the small side.** `churn_scores` and `suppression` are small versus `users`/`transactions`. Marking them `broadcast` ships them to every executor, turning a shuffle-heavy join into a map-side join — the easiest, biggest win. `autoBroadcastJoinThreshold` catches some of this automatically; be explicit for the ones you know.

**Watch data skew.** A few power users with millions of transactions, or a country holding 80% of users — one partition does all the work while the rest idle, and the whole job runs as slow as that one straggler. What helps: salt the hot keys, enable Adaptive Query Execution (`spark.sql.adaptive.enabled`), and pre-aggregate before the join (the `txn90` rollup above shrinks the big side first).

{/* IMAGE PROMPT (spark-data-skew.png): Clean conceptual diagram, flat vector, light background. A row of FOUR worker boxes labeled exactly "Worker 1", "Worker 2", "Worker 3", "Worker 4". Above each worker draw a vertical workload bar: the bar above Worker 1 is very tall (towering); the bars above Worker 2, Worker 3, Worker 4 are tiny. Place a small tag pointing at Worker 1 reading exactly "hot partition", and one tag near the small bars reading exactly "idle". Convey: data skew dumps all the work on one worker while the rest sit idle. STRICT: render ONLY these labels — "Worker 1", "Worker 2", "Worker 3", "Worker 4", "hot partition", "idle" — spelled exactly, each once; no other words; no numbers on the bars; no typos; no duplicate labels; no meta/style words. Flat vector, soft shadows, 2-3 accent colors, no photorealism. */}

![Data skew — one worker does all the work on a hot partition while the others idle](/assets/images/blogs/design-audience-platform/spark-data-skew.png)

**Prune at the source.** Two kinds of input, two kinds of pruning. `users` and `churn_scores` are daily snapshots — reading `dt=2026-07-01` picks up just today's copy. `transactions` is history partitioned by `event_date`, so the job reads the whole table path and lets the `event_date >= today-90` filter prune it down to the last 90 partitions — 90 days instead of five years, straight from the layout the last section set up.

<MermaidDiagram chart={`
flowchart LR
    L[("Data lake\n(users / txns / scores)")] --> S["Spark job\n(filter · join · aggregate)"]
    S --> M["Segment membership\n(user_id per segment)"]
`} />

This rung is where the audience platform stops being a small side project. A Spark cluster brings job failures, executor OOMs, and version upgrades of its own — in practice it arrives together with the first person whose actual job is running it. Budget the human, not just the cluster.

Spark fixes the compute; the lake means it never touches production again. What's left is cadence — how often does any of this actually run?

---

## Nightly isn't fresh enough — refresh every 1–2 hours

**The problem:** a day stale is too stale for a lot of rules. "Topped up this morning → eligible for the bonus banner by lunch, not tomorrow." Nightly means every segment is, on average, 12 hours behind reality.

**The fix:** don't jump to streaming. Move to **microbatch** — refresh every segment on a **1–2 hour cadence**, so nothing is more than an hour or two stale. Same freshness win as running the cron job more often back in the precompute rung, just made to hold up at data-lake scale. It's an approach, not a technology — scheduled batch jobs or a streaming engine in micro-batch mode both land at the same place. **The goal is getting eligibility right within your freshness target**, not a specific label.

Making it actually work:

- **Incremental, not full-rebuild.** A full rebuild every hour at data-lake scale won't finish inside its window. Read only what *changed* (Iceberg/Delta incremental reads, or the CDC feed), update the affected users, leave the rest. A heavier full rebuild still runs nightly to correct any drift.
- **Idempotent, snapshot-style publishes.** Each run publishes a versioned snapshot consumers switch to atomically — no half-updated segment ever visible, and a failed run just leaves the last good one in place.
- **Tier rules by freshness.** Most segments are fine on 1–2 hours. A few ("abandoned a cart 10 minutes ago") genuinely need seconds — that's the rare opt-in for true streaming (Flink, or a tight Structured Streaming trigger), not the default.

<MermaidDiagram chart={`
flowchart LR
    LAKE[("Data lake")] --> ETL["ETL (every 1-2h)\nincremental"]
    ETL --> SEG[("Segment results\nthousands-millions,\nrefreshed hourly")]
    EV["Event stream"] -.->|"rare real-time rules"| SEG
`} />

Now the audience is computed at scale and fresh within the hour. Last step: stop building this by hand per campaign, and serve it in the two shapes consumers need.

---

## Stop rebuilding per campaign — a reusable segmentation service

**The problem:** every segment is still a hand-written Spark job an engineer ships and maintains — a bottleneck past a handful of segments. The campaign platform (#7) can't self-serve audiences if every one needs an engineer.

**The fix:** turn hand-written jobs into a **platform**. Rules become *configuration*, not code:

- **Rules as a DSL / config.** A segment is data, not code — predicates, time windows, a freshness tier. `tier = gold AND country = TH AND avg_txn_90d > 5000 AND NOT suppressed`, stored, not coded. The platform validates and estimates size before anything runs.
- **One engine, generated jobs.** The platform compiles a rule into the actual Spark plan (and streaming topology, if tagged real-time). New attributes register once in a catalog and become available everywhere.
- **Reusability.** Segments compose — "gold_th_active" is computed once and referenced by ten campaigns, instead of re-derived per campaign — the v1 mistake, finally fixed.
- **Guardrails before the money moves.** A rule edit that flips `AND` to `OR` can quietly turn 2M users into 40M — a campaign about to spend real budget on 38M people who shouldn't get it. The platform holds any segment whose size jumps outside a sane band (±30%) until a human confirms it, before any consumer picks it up — the cheapest check against the single most expensive mistake this system can make.

Here's the payoff the whole post has been building to. The ETL's native output is the **segment result** — `campaign → [users]` — written to **object storage** as a versioned snapshot every 1–2 hours. That one artifact feeds both consumers, read in opposite directions:

<MermaidDiagram chart={`
flowchart TD
    ENG["Segmentation ETL"] --> SEG[("Segment results in object storage\ncampaign -> [users]")]
    SEG -->|"read + chunk"| OMNI["#3 Omnichannel\nstream the chunks out"]
    SEG -->|"invert (add/remove)"| MEM[("Membership index\nuser -> {campaigns}")]
    MEM --> ICON["#5 Icon serving\npoint lookup per request"]
`} />

- **Chunk read (#3 Omnichannel).** Reads the whole `campaign → [users]` list once, in order, and splits it into ~1K-user chunks — for a 40–60M audience, the ~40–60K chunks the omnichannel post sized its observability around. One sequential read, no random access.
- **Inverted index (#5 Icon serving).** Flips `campaign → [users]` into `user → {campaigns}` — a Redis set per user, `10001 -> {1, 2, 3, 4, 5}`. Back-of-envelope: 100M users, each holding a few campaign IDs, lands in the tens of GB — a modest cluster. Built as a step in the same ETL: each segment-result snapshot lands, then fans its users out into their per-user sets. This is exactly the membership store #5 read from, turned inside out.

{/* IMAGE PROMPT (inverted-index.png): Clean conceptual diagram, flat vector, light background. LEFT box header labeled exactly "Segment result" containing two text lines reading exactly "campaign 1 -> [10001, 10002]" and "campaign 2 -> [10001]". A bold arrow in the center labeled exactly "invert" points from LEFT to RIGHT. RIGHT box header labeled exactly "Membership index" containing two text lines reading exactly "10001 -> {1, 2}" and "10002 -> {1}". Convey: flipping campaign-to-users into user-to-campaigns. STRICT: render ONLY this text — "Segment result", "campaign 1 -> [10001, 10002]", "campaign 2 -> [10001]", "invert", "Membership index", "10001 -> {1, 2}", "10002 -> {1}" — spelled exactly, each once; no other words; no typos; no duplicate lines; no meta/style words. Flat vector, soft shadows, 2-3 accent colors, no photorealism. */}

![The inversion — campaign-to-users flipped into a per-user user-to-campaigns index for point lookups](/assets/images/blogs/design-audience-platform/inverted-index.png)

**Churn is the trap.** Each refresh adds and removes members — some users newly **match**, some **drop out**. Adds are easy: `SADD` the campaign to the user's set. Removals are the one people forget: skip the `SREM` and a user keeps an eligibility they've lost. Two ways to keep it honest:

- **Diff and apply** — compute the *added* and *removed* sets per segment, apply both. Cheap when churn is small, but leans on having the prior snapshot and getting the diff right. At scale, load both snapshots as **Roaring Bitmaps** and let bitmap subtraction do the math — `added = new.andNot(old)`, `removed = old.andNot(new)` — exact and fast even at tens of millions of IDs.
- **Rebuild and swap** — recompute every set fresh, swap it in atomically (shadow keyspace, flip a pointer). Self-correcting, no drift — at the cost of rewriting everything each cycle. Peak memory roughly **doubles** during the swap, since shadow and live coexist — size for that, or swap shard by shard.

Either way, object storage is the source of truth; Redis is a disposable, rebuildable view. One safety net on top of either strategy: TTL every per-user set slightly longer than the refresh cadence, so a missed removal ages out on its own instead of persisting until someone notices the wrong banner in production.

**A word on Bloom filters.** Tempting: shrink the check to a compact probabilistic structure instead of a full set. The catch is what *probabilistic* means — a Bloom filter never false-negatives, but it *will* false-positive at some rate, and for eligibility that means a user told they qualify for something they don't. Even a low rate becomes real business errors at volume. Eligibility stays exact; Bloom filters earn their keep only where a false positive is *harmless* — a pre-check before a lookup you'll verify anyway — never as the final answer.

{/* IMAGE PROMPT (bloom-filter-false-positive.png): Clean conceptual diagram, flat vector, light background. CENTER: a funnel/filter shape labeled exactly "Bloom filter". From the LEFT, two user icons approach: one tagged exactly "member" and one tagged exactly "non-member". BOTH pass through the filter to the RIGHT side. The "member" path ends in a green check; the "non-member" path ends with a red warning mark tagged exactly "false positive", leading to a box on the far right labeled exactly "wrong eligibility". Convey: a Bloom filter wrongly lets a non-member through, producing wrong eligibility. STRICT: render ONLY these labels — "Bloom filter", "member", "non-member", "false positive", "wrong eligibility" — spelled exactly, each once; user icons carry no extra text; no other words; no typos; no duplicate labels; no meta/style words. Flat vector, soft shadows, 2-3 accent colors, no photorealism. */}

![A Bloom filter lets a non-member slip through as a false positive — wrong eligibility, which is why it doesn't belong in the final answer](/assets/images/blogs/design-audience-platform/bloom-filter-false-positive.png)

One segment result, read two ways — sequential for delivery, inverted for serving. That's the reason audience deserves to be its own system, not a query attached to each sender.

{/* IMAGE PROMPT (audience-two-shapes.png): Clean conceptual diagram, flat vector, light background. CENTER: a single rounded box labeled exactly "Audience engine". From it, TWO arrows branch out. LEFT arrow goes to a stack of small identical rectangles (a chunked list) labeled exactly "Chunked list" with a sublabel below reading exactly "Omnichannel". RIGHT arrow goes to a cylinder/database shape labeled exactly "Membership store" with a sublabel below reading exactly "Icon serving"; inside or beside the cylinder show one tiny example line of text reading exactly "10001 -> {1,2,3}". Convey: one engine, two output shapes. STRICT: render ONLY these labels — "Audience engine", "Chunked list", "Omnichannel", "Membership store", "Icon serving", "10001 -> {1,2,3}" — spelled exactly, each once; no other words; no typos; no meta/style words. Flat vector, soft shadows, 2-3 accent colors, no photorealism. */}

![One audience engine materialized two ways — a chunked list for omnichannel delivery and a per-user membership store for icon serving](/assets/images/blogs/design-audience-platform/audience-two-shapes.png)

---

## Wrapping up

We started with a `WHERE` clause typed at send time and ended with a platform that computes audiences over a data lake and serves them two ways. The path, in order:

1. **Inline SQL at send time** — fine until the query is 40M rows on the production DB, mid-send.
2. **Precompute on a schedule** — build the segment ahead (idempotently, with a staleness alarm), read it cheap at send; but it still pounds the OLTP DB.
3. **A read replica** — the cheap rung most write-ups skip; split reads and the segment's output off the primary, no new engine yet.
4. **An analytical engine** — once the workload is real analytics (OLTP vs. OLAP), a columnar DB or warehouse runs the same SQL far faster; plenty of teams rightly stop here.
5. **A data lake** — Parquet + a table format, partitioned and ingested, landing a dozen systems' data in one place.
6. **Spark** — for when the rules themselves need to be code, not SQL.
7. **Refresh every 1–2 hours** — incremental ETL keeps thousands-to-millions of segment results fresh within the hour; true streaming stays the rare opt-in.
8. **A reusable segmentation service** — rules as config, one engine, guardrails on segment size, one segment result read two ways: chunked for delivery, inverted into a per-user index for serving.

The meta-point, same as the rest of the series: **nobody starts here.** A few thousand users and a simple rule? Type the `WHERE` clause and move on. Every layer above earned its place — scale forced precomputation, load forced the replica, real analytics forced the warehouse, fragmented data forced the lake, code-shaped rules forced Spark, recency forced microbatch, and repetition forced the platform. And each climb had a trigger you can measure — the batch that visibly slows live traffic, the job that can't finish inside its refresh window — not a technology that looked interesting.

And if there's one thing to carry over: the audience platform's job isn't just *computing* the right set of users — it's serving that set in the shape each consumer actually reads. Omnichannel streams it; icon serving point-looks-it-up. Get the computation right and pick the wrong serving shape, and you've built a brilliant brain that can't talk to either mouth.
