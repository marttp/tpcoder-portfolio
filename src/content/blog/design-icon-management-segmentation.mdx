---
title: "Designing a Personalized Icon & Asset Delivery System (Start Simple, Then Evolve)"
description: "Evolve a static, client-baked icon grid into a per-user asset delivery system — permissions, segmentation, eligibility with fallback — then split into a cache-first serving path and a relaxed backoffice."
pubDate: "2026-06-29"
tags:
  ["Software Architecture", "Software Engineering", "Backend"]
heroImage: "/assets/images/blogs/design-icon-management-segmentation/slot-anatomy.png"
language: "en"
translationSlug: "design-icon-management-segmentation-th"
servicesNote: true
---

import MermaidDiagram from '../../components/blog/MermaidDiagram.astro';

## Introduction

This is the fifth post in the backend system design series — same format as always: start with the most naive thing that works, then let real-world constraints push it into shape.

This one's shaped a little differently from the earlier posts, though. It ends up as **two systems in one** — with deliberately opposite priorities — so before the usual step-by-step evolution, let me set the mental model.

Picture the home screen of a super-app: the grid of icons, the nav bar, the promo banners. The unit we actually care about is the **slot**. A page is made of **blocks**, each block holds **slots**, and a slot is where a single icon / asset / banner renders. The *layout* — which blocks, which slots, where they sit — is a design choice you make up front. What *fills* each slot is the interesting part: it's resolved **per user**. User A sees asset A in a slot, user B sees asset B in that same slot, and a user who matches nothing gets the slot's **default**.

So the whole job, in one line: given a `(user, page)`, resolve every slot to an asset — or its default. Sounds small. It stops being small the moment you add permissions, segmentation, eligibility rules, and a few thousand requests a second that can never, ever render a blank screen.

Here's the road. The first half is the evolution that *forces* personalization on you; the second half is the two systems it splits into.

The evolution:

1. **Static icons** baked into each client
2. Permissions arrive → **show/hide per user**
3. A per-user layout can't live in the client → **serve it from an API**
4. Different users, different assets → **segments + tiers**
5. Never render a blank slot → **eligibility + fallback**

Then the split — two systems with opposite SLAs:

- **Front-facing serving** — high-RPS, latency-critical, must-never-blank. **Availability first.**
- **Backoffice** — internal, low-traffic, correctness-first. A few minutes of staleness is fine.

The punchline of the whole post is that contrast: the same domain pulls apart into two services you tune *differently*. Let's start simple.

---

## The naive version — icons baked into each client

You have to start somewhere, and the honest start is: hardcode the icon grid and the nav bar straight into each client. The mobile app ships a list of icons; the web app ships its own. No backend involved — the layout is just part of the build.

```
// mobile build — hardcoded
val homeIcons = listOf("wallet", "scan", "transfer", "rewards", "topup")
```

For a small app this genuinely works — ship it. Everyone sees the same grid, it renders instantly, and there's nothing to operate.

But two cracks show up fast:

- **Every change is a release.** Want to swap an icon, reorder the grid, or run a banner next week? That's an app-store submission (and its review wait) on mobile, plus a deploy on web. Marketing moves faster than your release train.
- **The platforms drift.** Web and mobile each hold their own copy of the truth, so they fall out of sync — a banner that's live on web but missing in the app, an icon mobile dropped but web still shows.

{/* IMAGE PROMPT (static-client-drift.png): Clean conceptual diagram, flat vector, light background. Two phone frames side by side. Left frame labeled exactly "Web", right frame labeled exactly "Mobile". Both show a grid of blank icon shapes, but the two grids visibly differ (one icon present on Web is absent on Mobile; a wide banner present on Mobile is absent on Web). Between them a small jagged divider labeled exactly "drift". STRICT: render ONLY these labels — "Web", "Mobile", "drift" — spelled exactly, each once; icons/banners are blank shapes with no text; no other words; no typos; no meta/style words. Flat vector, soft shadows, 2-3 accent colors, no photorealism. */}

![Web and mobile each hardcode their own set, so the two drift out of sync](/assets/images/blogs/design-icon-management-segmentation/static-client-drift.png)

Both cracks point the same way: the layout shouldn't live in the client. But before we move it, something else forces our hand first.

---

## Permissions arrive — show/hide per user

**The problem:** features start carrying permissions. A merchant tool only for merchant accounts, a "loans" entry only where lending is enabled, an admin shortcut only for staff. Suddenly the grid isn't one fixed list — what belongs on the screen depends on *who's looking*.

You *could* keep this in the client: ship every icon, then hide the ones the user can't use. That breaks two ways. You're shipping the existence of entries to people who shouldn't even know they're there (open the bundle and read them off), and the client now needs the full permission picture to make the call — which means it's making an authorization decision it has no business making.

**The fix:** resolve visibility on the server, per user. For a given user, walk the catalog of slots and keep only the ones their level / role / entitlements allow. The client stops deciding; it just renders what it's handed.

<MermaidDiagram chart={`
flowchart LR
    U["User + permissions"] --> R["Resolver"]
    CAT["Slot catalog (all)"] --> R
    R --> OUT["Slots this user may see"]
`} />

The shift that matters here isn't the filtering itself — it's that the answer is now **per-user**. The same page, requested by two different people, comes back as two different layouts. And a per-user answer is exactly the thing you can't bake into a client build.

{/* IMAGE PROMPT (permission-visibility.png): Clean conceptual diagram, flat vector, light background. Two phone frames side by side showing the SAME home grid but with different icons visible. Left frame labeled exactly "Staff user" shows one extra highlighted icon. Right frame labeled exactly "Regular user" has an empty gap where that icon would be. Convey: same page, permissions show/hide an icon per user. STRICT: render ONLY these labels — "Staff user", "Regular user" — spelled exactly, each once; icons are blank shapes with no text; no other words; no typos; no meta/style words. Flat vector, soft shadows, 2-3 accent colors, no photorealism. */}

![Same page, two users — permissions reveal an extra icon to one and hide it from the other](/assets/images/blogs/design-icon-management-segmentation/permission-visibility.png)

---

## A per-user layout can't live in the client — serve it from an API

**The problem:** the layout is per-user now, but it's still being assembled on the device. To do that the client has to download the whole catalog *and* the rules to filter it — which is slow, leaks what it shouldn't, and still can't change without a release. The v1 cracks and the v2 per-user need collide right here.

**The fix:** move resolution behind an API. The client calls one endpoint with its auth, the server resolves the slots for that user, and the response *is* the finished layout — what to render, in order, nothing else.

```
GET /pages/home          (auth: this user)
->
{
  "blocks": [
    { "id": "shortcuts", "slots": [
      { "id": "s1", "asset": { "icon": "wallet",   "deeplink": "app://wallet" } },
      { "id": "s2", "asset": { "icon": "scan",     "deeplink": "app://scan"   } }
    ]}
  ]
}
```

The client goes dumb on purpose: it draws blocks and fills slots with whatever came back. A few things fall out of this for free:

- **Change without a release** — swap an icon or a banner by changing data, not by shipping an app.
- **Web and mobile stop drifting** — one resolver, one source of truth; the response just carries a per-platform shape (mobile gets its grid, web gets its layout).
- **Catalog and rules stay server-side** — the client never sees what it isn't allowed to.

This is the serving API in its first form. It's also where the whole rest of the post lives: every step from here makes that one `(user, page) → layout` call either smarter, faster, or harder to knock over.

---

## Different users, different assets — segments and tiers

**The problem:** permissions only answer yes/no — *can* this user see this slot at all? The business wants more. The same slot should show **different assets** to different people: a gold-tier user gets a premium-rewards banner, a brand-new user gets an onboarding nudge, a lapsed user gets a win-back promo. Same slot, same position on screen — different content per audience.

**The fix:** make slot resolution a *lookup*, not just a filter. Each slot has a set of candidate assets, and each candidate is tagged with the audience it's meant for — a tier, some attributes, an active campaign. At request time, for each slot, you pick the asset whose targeting matches this user.

A few things worth being precise about:

- **Targeting is data, not code.** "gold tier, in TH, signed up under 30 days ago" is a rule attached to an asset and evaluated against the user's attributes — so the business can add an audience without a deploy.
- **Priority matters when more than one matches.** A user can fall into several audiences at once, so you need a deterministic order (most-specific wins, or an explicit rank). Otherwise the slot shows whichever row the database happened to return first — different on every request.
- **A campaign is just a time-boxed asset** — targeting plus a start/end window. "The right banner for this user" becomes: the highest-priority asset whose targeting matches *and* whose window is open right now.

{/* IMAGE PROMPT (per-user-slot-assets.png): Clean conceptual diagram, flat vector, light background. One slot position shown three times (the same square outline), each filled with a DIFFERENT colored banner/asset shape, labeled underneath exactly "User A", "User B", "Default". A small tag on User A reads exactly "gold tier", on User B reads exactly "new user", on Default reads exactly "no match". Convey: same slot, a different asset per audience, default when nothing matches. STRICT: render ONLY these labels — "User A", "User B", "Default", "gold tier", "new user", "no match" — spelled exactly, each once; asset shapes carry no other text; no typos; no meta/style words. Flat vector, soft shadows, 2-3 accent colors, no photorealism. */}

![Same slot, resolved per audience — User A, User B, and the default each get a different asset](/assets/images/blogs/design-icon-management-segmentation/per-user-slot-assets.png)

So a slot's answer for one user is: of all candidates for that slot, take the matching, eligible, highest-priority one. Which begs the obvious question — what happens when **nothing** matches?

---

## Never render a blank slot — eligibility + fallback

**The problem:** now that each slot is a targeted lookup, plenty of users will match nothing — and plenty of *requests* will fail mid-resolution. The eligibility check times out, the targeting store blips, a campaign ended a second ago. On a home screen at thousands of requests a second, every one of those turns into a hole in the grid — a blank slot, or worse, a blank page. That's the one outcome you can't ship: the app opening to nothing.

**The fix:** every slot carries a **default** asset, and resolution is wrapped so it always returns *something*. Two cases, one rule:

- **No match (ineligible)** — the user fits no targeted candidate → render the slot's default.
- **Failure (timeout / dependency down)** — resolution couldn't finish inside its budget → render the slot's default.

One caveat: the default itself can be **blank**. If a slot's default is empty, that slot renders nothing — it collapses, on purpose. That's different from a hole caused by a failure: it's a blank you designed, not data you dropped on the way.

<MermaidDiagram chart={`
flowchart TD
    S["Resolve slot for user"] --> M{"Match + eligible?"}
    M -->|"yes"| A["Render matched asset"]
    M -->|"no"| D["Render slot default"]
    S -.->|"timeout / error"| D
`} />

The mindset shift: the request path's job isn't "compute the perfect asset." It's "return a good-enough asset, always, fast." Personalization is the upside; the default is the floor you never fall through. Put a tight timeout on the fancy path and let it fail *to the default*, never to an error.

{/* IMAGE PROMPT (campaign-window.png): Clean conceptual timeline, flat vector, light background. A horizontal axis labeled exactly "time". A shaded interval in the middle labeled exactly "campaign window", with edge markers labeled exactly "start" and "end". Above the shaded interval a slot shows a promo banner labeled exactly "campaign asset"; before "start" and after "end" the same slot shows a plain banner labeled exactly "default". Convey: the campaign asset shows only inside the window; outside it the slot falls back to default. STRICT: render ONLY these labels — "time", "campaign window", "start", "end", "campaign asset", "default" — spelled exactly, each once; no other words; no typos; no meta/style words. Flat vector, soft shadows, 2-3 accent colors, no photorealism. */}

![A campaign is a time-boxed asset — shown inside its window, default outside it](/assets/images/blogs/design-icon-management-segmentation/campaign-window.png)

It's the same instinct as the webhook post's "never lose an event" — here it's "never render a blank." And it's the hinge that splits this into two systems. The moment *always fast, always something* becomes the contract on the read path, that path wants completely different treatment from the backoffice that *manages* the assets. So let's pull them apart.

---

## Pull it apart — two systems, opposite priorities

Everything so far has been one `(user, page) → layout` call. But two very different jobs hide inside it: **serving** the layout (read, hot, every request) and **managing** the assets and rules behind it (write, rare, internal). They pull in opposite directions, so we build them as two systems with one thin connection.

<MermaidDiagram chart={`
flowchart LR
    UI["Operator UI"] --> SOT[("Source of truth\nrules + assets")]
    SOT --> W["Cache warmer /\ninvalidation"]
    W --> C[("Serving cache")]
    API["Serving API"] --> C
    CL["Client"] --> API
`} />

Same domain, mirror-image requirements:

| | **Front-facing serving** | **Backoffice** |
| --- | --- | --- |
| Traffic | thousands of RPS | a handful of operators |
| Priority | availability + latency | correctness + usability |
| Shape | read-heavy | write / config |
| Freshness | instant, every request | minutes of staleness is fine |
| Cost of failure | a blank screen for users | an operator waits and retries |

{/* IMAGE PROMPT (two-systems-contrast.png): Clean conceptual diagram, flat vector, light background. Two mirror-image panels split by a thin vertical divider. Left panel header labeled exactly "Front-facing serving" with three small stacked tags labeled exactly "high RPS", "low latency", "never blank". Right panel header labeled exactly "Backoffice" with three small stacked tags labeled exactly "low traffic", "correctness", "minutes is fine". STRICT: render ONLY these labels — "Front-facing serving", "Backoffice", "high RPS", "low latency", "never blank", "low traffic", "correctness", "minutes is fine" — spelled exactly, each once; no other words; no typos; no meta/style words. Flat vector, soft shadows, 2-3 accent colors, no photorealism. */}

![Two systems, opposite priorities — serving tunes for speed and availability, the backoffice for correctness](/assets/images/blogs/design-icon-management-segmentation/two-systems-contrast.png)

The rest of the post is designing each to its own contract — and being honest that the backoffice gets to be the *simpler* one, because we let it.

---

## System A — Front-facing serving (the read path)

This is the side that eats the thousands of RPS, so the whole design bends toward one sentence: **answer from memory, and never block on anything slow.**

**Serve from a cache, not a live computation.** Resolving a user's layout touches the rule store, the campaign/eligibility data, and the asset catalog — far too much to do per request at this volume. So the heavy lifting — rule evaluation, candidate ordering — happens *ahead* of the request and lands in a cache; the request path only makes a cheap final pick over that pre-built data.

**A slot resolves by first-eligible-wins.** A slot isn't one asset — it's an *ordered list of candidates*, and resolution is just a short walk down it:

- Candidates are sorted by priority ahead of time and cached in that order.
- At request time the serving API walks the list from the top and stops at the first candidate the user is eligible for — no scoring the whole list, no hunting for a "best" match.
- Example: a user qualifies for campaigns 1, 2, and 5, and a slot's candidates are `[campaign 2 banner, campaign 5 banner, default]` → the loop takes campaign 2's banner on the first eligible hit and never looks further.

**Where eligibility comes from.** The serving cache holds the *candidates*, not the verdict on who qualifies:

- It stores each slot's ordered assets and the campaign each one is bound to — but it doesn't decide eligibility.
- That comes from the Audience domain (its own post, next in this series): a rule engine evaluates the targeting rules and writes the result into a membership store keyed by user — in practice a Redis set per user of the campaigns they currently qualify for, e.g. `10001 -> {1, 2, 3, 4, 5}`.
- So a slot resolves with **two memory reads**: the ordered candidates (serving cache) and the user's eligible set (membership store). Banner A is bound to campaign 1, so it's eligible for user 10001 because 1 is in their set.

<MermaidDiagram chart={`
flowchart TD
    A["Request: user + slot"] --> B["Ordered candidates\n(serving cache)"]
    A --> C["Eligible campaign set\n(membership store)"]
    B --> D{"Top candidate's campaign\nin eligible set?"}
    C --> D
    D -->|"yes"| E["Pick it, stop"]
    D -->|"no"| F["Next candidate"]
    F --> D
    F -.->|"none left"| G["Slot default"]
`} />

**A worker keeps the cache warm, asynchronously.** A separate cache-warming worker — one deployable, or several partitioned by page/segment — reads the source of truth and (re)builds the resolved view: per segment where it can, per user where it must. Serving and warming never share a request. Warming is background work; serving is a memory read.

<MermaidDiagram chart={`
flowchart LR
    SOT[("Source of truth")] --> WK["Cache warmer\n(background)"]
    WK --> C[("Serving cache")]
    CL["Client"] -->|"GET /pages/home"| API["Serving API"]
    API -->|"read"| C
    API -.->|"miss / error"| DEF["Slot default"]
`} />

**Overwrite the cache, never delete it.** When the warmer rebuilds an entry, the only mutation it's allowed is write-on-success. If a rebuild fails validation or can't load its inputs, it leaves the existing entry exactly where it is. A stale-but-valid view still serves the user; a deleted key serves a blank (or stampedes everyone onto the slow path at once). The serving cache only ever moves forward by overwrite — deletion is never on the table.

**Gate the deploy on a warm cache.** Here's the detail people miss: when a new serving instance boots, it has to finish loading its cache **before** it's marked ready and put into rotation. A half-warm instance that starts taking traffic serves blanks (or falls through to the slow path) for its first minutes — exactly during a rollout, the worst moment. So readiness *is* "cache loaded." The tradeoff is honest: deploys get slower, and a big dataset makes cold start long — so warm from a **snapshot** instead of recomputing from scratch on every boot.

{/* IMAGE PROMPT (deploy-gate.png): Clean conceptual diagram, flat vector, light background. Two stacked states of the same scene. Each scene: a server box labeled exactly "New instance" with a progress bar inside, a gate/barrier in front of it, and an incoming arrow labeled exactly "traffic". Top state: the bar is half-filled labeled exactly "loading cache", the gate is CLOSED, the traffic arrow is blocked. Bottom state: the bar is full labeled exactly "cache ready", the gate is OPEN, the traffic arrow flows through into the box. Convey: an instance takes traffic only after its cache is fully warm. STRICT: render ONLY these labels — "New instance", "traffic", "loading cache", "cache ready" — spelled exactly; no other words; no typos; no meta/style words. Flat vector, soft shadows, 2-3 accent colors, no photorealism. */}

![A new instance stays out of rotation while its cache loads, and serves only once it's warm](/assets/images/blogs/design-icon-management-segmentation/deploy-gate.png)

**Fall back to the default, always.** Even reading from cache, a key can miss — a brand-new user, a just-added slot. The read path still ends in the v5 rule: miss or doubt → the slot default. The cache makes you fast; the default keeps you safe.

**Where the cache lives is a real decision:**

- **In-memory, per instance** — the fastest possible read (no network hop), but every instance holds the whole set and every deploy re-warms it. Before you commit, do the back-of-the-envelope: you're storing the *resolved JSON* — CDN URLs plus slot metadata, not the image bytes — so size it as (number of cached views) × (a slot's worth of JSON). One slot entry is maybe a few hundred bytes; a page of a dozen slots, a few KB. Per segment that stays tiny; cache it *per user* and it becomes users × pages, which is where it stops fitting in RAM. Fine when the resolved set fits in memory and you can snapshot it for a quick load.
- **Shared (Redis / Memcached)** — one warm copy that many instances read; deploys don't re-warm, but you pay a network hop per read and the cache is now a dependency to keep alive.

{/* IMAGE PROMPT (cache-location.png): Clean conceptual comparison, flat vector, light background, two panels. Left panel header labeled exactly "In-memory per instance": three server boxes, each containing its OWN small cache box (no shared store), with a tag labeled exactly "fastest, re-warms on deploy". Right panel header labeled exactly "Shared cache": three server boxes all drawing arrows to ONE central cache cylinder labeled exactly "Redis", with a tag labeled exactly "warm once, network hop". STRICT: render ONLY these labels — "In-memory per instance", "Shared cache", "Redis", "fastest, re-warms on deploy", "warm once, network hop" — spelled exactly, each once; no other words; no typos; no meta/style words. Flat vector, soft shadows, 2-3 accent colors, no photorealism. */}

![Cache-location tradeoff — in-memory per instance versus one shared cache](/assets/images/blogs/design-icon-management-segmentation/cache-location.png)

Plenty of setups land on a blend: a shared cache as the warmed source, plus a small in-process layer for the hottest keys. Whichever way, the theme holds — the read path does as little as possible, as fast as possible, and degrades to a default rather than an error.

---

## System B — Backoffice (the write/config path)

This is where operators actually do the work: create assets, define audiences, map a campaign to a slot, set its window. It's internal, low-traffic, and — the key decision — **allowed to be eventually consistent.** A change doesn't have to hit screens this millisecond; a few minutes of propagation is fine. That one permission is what lets the backoffice stay simple.

**An internal UI over the source of truth.** Operators manage icons / banners / campaigns and map them to segments, placements (which slot), and schedules. It's plain CRUD against the rule + asset store — no exotic infrastructure. Correctness and clear validation matter far more here than latency or uptime.

**Getting an asset in — three paths.** An asset is an image plus metadata, and there's more than one way it arrives:

- **Managed upload** — the operator uploads the file → you **validate** it (format, size, dimensions) → store it in object storage → serve it via CDN, with the final CDN URL filling in **asynchronously** (so the UI shows "processing" for a beat, not a broken link).
- **Bring-your-own URL, used directly** — the operator pastes a link they already host, and you store it as-is. Fast and zero-effort, but you inherit their problem: a broken link, a slow host, an asset that quietly disappears — and now it's *your* blank slot.
- **Bring-your-own URL, ingested** — the operator pastes a link, but in the background you download it, re-upload to your own object storage, and serve from your CDN. You own availability and caching from then on; the external host only mattered once.

The middle option is the honest trap — easiest to ship, first to page you. Which paths you allow is a call; just make it a deliberate one.

{/* IMAGE PROMPT (ingestion-paths.png): Clean conceptual diagram, flat vector, light background. An operator figure on the left labeled exactly "Operator" feeding into THREE horizontal lanes that each end at a slot on the right. Lane 1 header labeled exactly "Managed upload" with a sequence of boxes labeled exactly "upload", "validate", "object storage", "CDN". Lane 2 header labeled exactly "BYO URL - direct" with a single straight arrow to the slot and a small warning mark labeled exactly "external risk". Lane 3 header labeled exactly "BYO URL - ingest" with boxes labeled exactly "download", "re-upload", "CDN". STRICT: render ONLY these labels — "Operator", "Managed upload", "upload", "validate", "object storage", "CDN", "BYO URL - direct", "external risk", "BYO URL - ingest", "download", "re-upload" — spelled exactly ("CDN" may appear in lane 1 and lane 3); no other words; no typos; no meta/style words. Flat vector, soft shadows, 2-3 accent colors, no photorealism. */}

![Three ways an asset gets in — managed upload, bring-your-own-URL used directly, or BYO-URL ingested](/assets/images/blogs/design-icon-management-segmentation/ingestion-paths.png)

**Publishing = invalidate / refresh the cache.** When an operator saves, the write lands in the source of truth — and that's where the backoffice's job ends. A signal goes to the serving side (invalidate the affected keys, or nudge the warmer to rebuild) and the new state shows up on screens shortly after. "Shortly" is the whole point: this is the ~10-minutes-is-fine SLA in action. No need to make the backoffice highly available or low-latency; it isn't on anyone's hot path.

A fun aside: you might be tempted to make this strongly consistent — operator hits save, the cache updates in the same breath, guaranteed fresh. I don't think that's a good direction. It drags the relaxed backoffice back under the serving path's tight SLA and re-couples the two systems we spent the whole post pulling apart. The cache is already enough of a headache on its own; don't sign it up for a distributed transaction too. Lean on the async refresh and the default — eventual is plenty here. 😅

So the backoffice is deliberately the boring one: a UI, some validation, writes to a store, and a poke at the cache. Boring is the *correct* amount of engineering for it.

---

## The connection point — where the two systems touch

Two systems, but not fully independent — there's exactly one wire between them, and keeping it to *one* wire is what makes the split clean.

The connection point: **backoffice writes the source of truth → a signal (invalidate or rebuild) → the warmer refreshes the serving cache.** That's the whole connection. The backoffice never calls the serving API; the serving path never reads the backoffice's database directly. They meet only through the source of truth and the cache sitting between them.

Two things make this comfortable to live with:

- **It's asynchronous and tolerant of a miss.** The signal can be a queue message, a CDC stream, or just the warmer re-reading on a timer. Miss one refresh and the next sweep catches it — because the serving side always has a safe default to show in the gap. The ~10-minute SLA is exactly the slack that lets this stay simple.
- **The source of truth is the contract.** As long as both sides agree on the shape of a resolved view, you can rebuild, re-shard, or redeploy either side without touching the other.

Same lesson as the webhook split: keep the coupling to one well-understood connection point, and each side is free to be tuned — or to fail, or to scale — on its own.

---

## Last but not least — GenAI for generated content

Strip this system down and it's about putting the right *piece of content* in the right slot for the right person — and generating content is exactly what GenAI is built for.

However, I would recommend it lives on the **backoffice** side, not the serving side. The read path is cache-heavy and latency-bound — no place for a slow, nondeterministic model call. The backoffice is low-stress, human-reviewed, relaxed SLA. So GenAI sits there: an operator generates, reviews, publishes, and the result flows through the source of truth into the cache like any other asset. It's effectively a fourth ingestion path next to upload / BYO-direct / BYO-ingest — *generate*.

The sweet spot is generating **themed sets**, not one-offs:

- A house/company theme — one consistent visual language across every icon and banner.
- Predefined themes by function — wallet, credit card, nearby store, a mini-app's own look.
- Predefined themes by season — Halloween, spring, winter.

An operator picks a theme, the model drafts the set, a human approves, the warmer caches it. Personalization here means choosing a *theme per segment* — which stays perfectly cacheable, because it's still a handful of sets, not one per user.

The trap is letting it run all the way to **per-user custom icons**. Don't. That's the cache-killer this whole post has been dodging — it drags you straight back to users × slots, a set too large to precompute or cache, and an ops headache on top. On high-traffic surfaces, keep generated personalization at theme/segment granularity.

There *is* a place for one-of-a-kind, per-user generated visuals — just not the home grid. Surfaces that rarely update and sit off the hot path — a payment-complete screen, a virtual-card theme — can afford to generate once per user and store it, because it doesn't churn and isn't read thousands of times a second. The deciding question is the one this post keeps asking: how often does it change, and how hard is it to cache?

---

## Wrapping up

We started with icons hardcoded into each client and ended with two systems that share one cache. The path, in order:

1. **Static icons in the client** — fine until every change is a release.
2. **Permissions** make the layout per-user — the forcing function.
3. **A per-user layout can't live in the client** → serve it from an API.
4. **Segments + tiers** turn each slot into a targeted lookup.
5. **Eligibility + fallback** — every slot has a default, so the screen is never blank.
6. **Split into two systems** — a cache-first serving path (warmed before it serves) and a low-criticality backoffice, joined by one async connection point.

The meta-point, same as the rest of the series: **nobody starts here.** One app, one fixed grid? Hardcode it and move on. Every layer above earned its place by solving a problem the previous one created — permissions forced the API, scale forced the cache, and the opposite-SLA reality forced the split.

If there's one thing to carry over: the serving and backoffice halves *look* like the same domain, but they're different problems — and the whole win is in letting them be different. The read path obsesses over latency and never-blank; the backoffice gets to be calm, correct, and a few minutes behind. Hold both to the same bar and you over-build one while under-serving the other.
