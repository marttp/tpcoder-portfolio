---
import Layout from '../layouts/BaseLayout.astro';
import SectionTitle from '../components/SectionTitle';
import experiences from '../data/experiences.json';
import { ProfessionalExperience } from "../data/props";

const professionalExperiences: ProfessionalExperience[] = experiences.professional;
---

<Layout
  title="Experiences"
  description="Thanaphoom Babparn experiences along the career path"
>
  <section>
    <SectionTitle title="Professional Experiences" />
    <ul class="list-none">
      {
        professionalExperiences.map((exp, index) => (
          <li class="mb-8 rounded overflow-hidden shadow-lg dark:shadow-primary-light p-8">
            <h3 class="text-xl font-bold">{exp.position}</h3>
            <h4 class="text-lg font-bold">
              {exp.company} - {exp.country}
            </h4>
            <p class="text-gray-700">
              {exp.startDate} - {exp.endDate}
            </p>
            <ul class="list-disc list-inside mt-2 px-8">
              {exp.highlights.map((highlight, index) => (
                <li>{highlight}</li>
              ))}
            </ul>
          </li>
        ))
      }
    </ul>
  </section>
</Layout>
